{
  "name": "axyon-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check api/create-order.js && node --check api/capture-order.js && node --check api/paypal-webhook.js && node --check public/js/app.js",
    "dev": "npx --yes vercel dev",
    "deploy": "npx --yes vercel deploy --prod"
  }
}
